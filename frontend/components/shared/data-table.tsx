import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { cn } from '@/lib/utils';

export function DataTable({ headers, children, empty = false }: { headers: string[]; children?: React.ReactNode; empty?: boolean }) { return <div className="surface overflow-hidden rounded-lg"><div className="overflow-x-auto"><Table><TableHeader><TableRow className="hover:bg-transparent">{headers.map((header) => <TableHead key={header} className="whitespace-nowrap text-[11px] uppercase tracking-[0.08em]">{header}</TableHead>)}</TableRow></TableHeader><TableBody>{empty ? <TableRow><TableCell colSpan={headers.length} className="h-40 text-center text-muted-foreground">No records found.</TableCell></TableRow> : children}</TableBody></Table></div><div className={cn('flex items-center justify-between border-t border-border/70 px-4 py-3 text-xs text-muted-foreground', !children && 'hidden')}><span>Showing 1–6 of 24 records</span><div className="flex items-center gap-1"><button className="rounded border border-border px-2 py-1 hover:bg-muted">Previous</button><button className="rounded border border-primary/30 bg-primary/10 px-2 py-1 text-primary">1</button><button className="rounded border border-border px-2 py-1 hover:bg-muted">2</button><button className="rounded border border-border px-2 py-1 hover:bg-muted">Next</button></div></div></div> }
