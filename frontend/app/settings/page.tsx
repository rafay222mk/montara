'use client';

import { Bell, Building2, ChevronRight, LockKeyhole, Mail, SlidersHorizontal, UserRound } from 'lucide-react';
import { useState } from 'react';
import { AppShell } from '@/components/layout/app-shell';
import { PageHeader } from '@/components/shared';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Button } from '@/components/ui/button';
import { Switch } from '@/components/ui/switch';
import { useAuth } from '@/lib/auth/auth-context';

const sections = [{ id: 'profile', title: 'Your profile', description: 'Personal details and contact information', icon: UserRound }, { id: 'account', title: 'Account security', description: 'Email, password, and account access', icon: LockKeyhole }, { id: 'notifications', title: 'Notifications', description: 'Choose what updates you receive and when', icon: Bell }, { id: 'preferences', title: 'Application preferences', description: 'Regional settings, terms, and defaults', icon: SlidersHorizontal }, { id: 'school', title: 'School profile', description: 'Name, address, contact details, and branding', icon: Building2 }];

export default function SettingsPage() { const [active, setActive] = useState('profile'); const { user } = useAuth(); const current = sections.find((section) => section.id === active) || sections[0]; return <AppShell><PageHeader eyebrow="System / Workspace" title="Settings" description="Shape Montara around the way your school works." /><div className="grid gap-6 lg:grid-cols-[260px_1fr]"><div className="space-y-1">{sections.map(({ id, title, description, icon: Icon }) => <button key={id} onClick={() => setActive(id)} className={`flex w-full items-center gap-3 rounded-lg p-3 text-left transition-colors ${active === id ? 'bg-accent text-accent-foreground' : 'text-muted-foreground hover:bg-muted/70 hover:text-foreground'}`}><span className="flex h-9 w-9 items-center justify-center rounded-lg bg-muted"><Icon className="h-4 w-4" /></span><span className="min-w-0 flex-1"><span className="block text-sm font-medium">{title}</span><span className="mt-0.5 block truncate text-[11px] text-muted-foreground">{description}</span></span><ChevronRight className="h-4 w-4 shrink-0" /></button>)}</div><Card className="max-w-2xl"><CardHeader><CardTitle className="text-[15px]">{current.title}</CardTitle><p className="text-xs text-muted-foreground">{current.description}</p></CardHeader><CardContent>{active === 'profile' && <div className="space-y-5"><div className="flex items-center gap-4"><span className="flex h-14 w-14 items-center justify-center rounded-xl bg-primary/15 text-lg font-semibold text-primary">{user?.initials}</span><div><p className="text-sm font-medium">Profile photo</p><p className="text-xs text-muted-foreground">Visible to your school community</p></div><Button variant="outline" size="sm" className="ml-auto">Change</Button></div><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label>Full name</Label><Input defaultValue={user?.name} /></div><div className="space-y-2"><Label>Email address</Label><Input defaultValue={user?.email} /></div></div><Button>Save profile</Button></div>}{active === 'account' && <div className="space-y-5"><div className="space-y-2"><Label>Sign-in email</Label><div className="relative"><Mail className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><Input className="pl-9" defaultValue={user?.email} /></div></div><div className="space-y-2"><Label>Current password</Label><Input type="password" placeholder="Enter current password" /></div><div className="space-y-2"><Label>New password</Label><Input type="password" placeholder="Enter new password" /></div><Button>Update security</Button></div>}{active === 'notifications' && <div className="space-y-5">{[['Daily attendance summary', 'Receive a morning overview of attendance.', true], ['Learning updates', 'Be notified when new observations are recorded.', true], ['Finance reminders', 'Receive reminders about outstanding balances.', false], ['Weekly digest', 'Get a weekly summary of school activity.', true]].map(([title, description, checked]) => <div key={title as string} className="flex items-center justify-between border-b border-border/70 pb-4 last:border-0"><div><p className="text-sm font-medium">{title as string}</p><p className="mt-1 text-xs text-muted-foreground">{description as string}</p></div><Switch defaultChecked={checked as boolean} /></div>)}</div>}{active === 'preferences' && <div className="space-y-5"><div className="space-y-2"><Label>School year</Label><Input defaultValue="2024–2025" /></div><div className="space-y-2"><Label>Default term</Label><Input defaultValue="Term 3" /></div><div className="space-y-2"><Label>Timezone</Label><Input defaultValue="Eastern Time (ET)" /></div><Button>Save preferences</Button></div>}{active === 'school' && <div className="space-y-5"><div className="space-y-2"><Label>School name</Label><Input defaultValue={user?.schoolName} /></div><div className="space-y-2"><Label>School address</Label><Input defaultValue="100 Garden Avenue, Springfield" /></div><div className="space-y-2"><Label>School email</Label><Input defaultValue="hello@montara.school" /></div><Button>Save school details</Button></div>}</CardContent></Card></div></AppShell>; }
